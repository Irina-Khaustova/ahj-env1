// TODO: write code here

// comment this to pass build
const unusedVariable = 'variable';

// for demonstration purpose only
export default function demo(value2) {
  return value2;
}
demo(unusedVariable);
